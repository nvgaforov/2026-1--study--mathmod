---
author:
  name: Гафоров Нурмухаммад
  email: 1032234484@rudn.ru
  affiliation:
    - name: Российский университет дружбы народов
      country: Российская Федерация
      city: Москва
title: "Математическое моделирование"
subtitle: "Лабораторная работа №7"
license: "CC BY"
date: today
date-format: "YYYY-MM-DD"
---

# Вводная часть

## Цель работы

Исследовать модель эффективности рекламы и проанализировать, как информация о товаре распространяется среди потенциальных покупателей.

## Задание

1. Рассмотреть математическую модель рекламной кампании.
2. Построить графики распространения рекламы для трёх вариантов модели.
3. Проанализировать скорость изменения числа информированных покупателей.
4. Провести параметрическое исследование.
5. Сравнить динамику трёх моделей и выделить их основные особенности.

# Теоретические сведения

## Модель распространения рекламы

В модели используются следующие обозначения:

- $N$ — общее число потенциальных покупателей;
- $n(t)$ — количество покупателей, которые знают о товаре к моменту времени $t$;
- $t$ — время, прошедшее с начала рекламной кампании;
- $\frac{dn}{dt}$ — скорость распространения информации о товаре.

## Идея модели

Информация о товаре распространяется по двум каналам:

1. Через прямое рекламное воздействие.
2. Через общение покупателей между собой.

Общая модель записывается в виде:

$$
\frac{dn}{dt} =
(\alpha_1(t) + \alpha_2(t)n(t))(N - n(t)).
$$

## Смысл множителей

Множитель $(N - n(t))$ показывает число потенциальных покупателей, которые ещё не получили информацию о товаре.

Когда $n(t)$ приближается к $N$, выполняется соотношение:

$$
N - n(t) \rightarrow 0.
$$

Поэтому скорость распространения рекламы постепенно уменьшается.

## Уровень насыщения

Величина $N$ задаёт предельный уровень охвата аудитории:

$$
n(t) \rightarrow N.
$$

При $n = N$ все потенциальные покупатели уже знают о товаре, а дальнейшее распространение информации прекращается.

# Постановка задачи

## Исходные данные

Заданы параметры:

$$
N = 609,
$$

$$
n(0) = 4.
$$

Требуется построить графики зависимости $n(t)$ для трёх моделей распространения рекламы.

## Первая модель

$$
\frac{dn}{dt} =
(0.54 + 0.00016n(t))(N - n(t)).
$$

В первой модели основной вклад в распространение информации вносит рекламная кампания. Влияние общения между покупателями выражено слабее.

## Вторая модель

$$
\frac{dn}{dt} =
(0.000021 + 0.38n(t))(N - n(t)).
$$

Во второй модели ведущую роль играет передача информации между уже информированными и ещё неинформированными покупателями.

## Третья модель

$$
\frac{dn}{dt} =
(0.2\cos t + 0.2\cos 2t \cdot n(t))(N - n(t)).
$$

В третьей модели коэффициенты зависят от времени, поэтому интенсивность распространения информации меняется в процессе рекламной кампании.

# Базовые эксперименты

## Первая модель: динамика $n(t)$

![Первая модель: зависимость n(t)](image/single_experiment_case1.png)

## Первая модель: скорость изменения

![Первая модель: скорость изменения](image/single_experiment_case1_derivative.png)

## Первая модель: фазовая траектория

![Первая модель: фазовая траектория](image/single_experiment_case1_phase_n_dn.png)

## Анализ первой модели

Для первой модели наблюдается монотонное увеличение $n(t)$.

Основные особенности процесса:

- $n(t)$ постепенно приближается к уровню $N = 609$;
- наиболее быстрый рост происходит в начале расчёта;
- затем скорость распространения информации снижается;
- решение подходит к уровню насыщения снизу и не превышает его.

## Вывод по первой модели

Первая модель описывает насыщаемый рост числа информированных покупателей.

При приближении к уровню $N$ множитель $(N - n)$ уменьшается, поэтому:

$$
\frac{dn}{dt} \rightarrow 0.
$$

Состояние $n = N$ является равновесным, поскольку после полного охвата аудитории дальнейший рост невозможен.

# Вторая модель

## Вторая модель: динамика $n(t)$

![Вторая модель: зависимость n(t)](image/single_experiment_case2.png)

## Вторая модель: скорость изменения

![Вторая модель: скорость изменения](image/single_experiment_case2_derivative.png)

## Вторая модель: фазовая траектория

![Вторая модель: фазовая траектория](image/single_experiment_case2_phase_n_dn.png)

## Анализ второй модели

Во второй модели рост происходит значительно быстрее, чем в первой.

Кривая $n(t)$ имеет S-образный вид:

- в начале число информированных покупателей растёт медленно;
- затем скорость резко увеличивается;
- после достижения пика рост замедляется;
- решение быстро приближается к уровню $N$.

## Максимальная скорость

Во второй модели скорость распространения рекламы имеет выраженный максимум.

Этот максимум возникает из-за взаимодействия двух факторов:

- множитель $bn$ усиливает распространение информации;
- множитель $(N - n)$ ограничивает дальнейший рост.

Наибольшая скорость достигается внутри интервала изменения $n$, когда число информированных покупателей уже достаточно велико, но запас неохваченной аудитории ещё остаётся значительным.

# Третья модель

## Третья модель: динамика $n(t)$

![Третья модель: зависимость n(t)](image/single_experiment_case3.png)

## Третья модель: скорость изменения

![Третья модель: скорость изменения](image/single_experiment_case3_derivative.png)

## Третья модель: фазовая траектория

![Третья модель: фазовая траектория](image/single_experiment_case3_phase_n_dn.png)

## Анализ третьей модели

Третья модель также приводит систему к насыщению.

Её особенность связана с тригонометрическими коэффициентами:

$$
\cos t,\quad \cos 2t.
$$

На выбранном коротком интервале расчёта эти функции остаются положительными. Поэтому колебательный режим не появляется, а решение продолжает монотонно возрастать.

## Вывод по третьей модели

Третья модель описывает насыщаемый рост с коэффициентами, изменяющимися во времени.

Несмотря на наличие тригонометрических функций, общий характер динамики сохраняется:

$$
n(t) \rightarrow N.
$$

Система быстро выходит на уровень насыщения, после чего скорость изменения становится близкой к нулю.

# Сравнение базовых моделей

## Качественное различие

| Характеристика | case1 | case2 | case3 |
|---|---|---|---|
| Вид роста | монотонный | S-образный | S-образный |
| Скорость в начале | максимальная | малая | малая |
| Максимум $\frac{dn}{dt}$ | в начале | внутри интервала | внутри интервала |
| Предельное значение | $N$ | $N$ | $N$ |
| Коэффициенты | постоянные | постоянные | зависят от времени |

## Общая особенность

Во всех трёх моделях присутствует ограничивающий множитель:

$$
N - n(t).
$$

Он отвечает за насыщение и не позволяет числу информированных покупателей расти неограниченно.

# Параметрическое исследование

## Зависимость $n_{final}$ от параметра $a$

![Зависимость итогового значения n от параметра a](image/n_final_vs_a.png)

## Анализ зависимости $n_{final}(a)$

Большинство точек расположено около уровня $N = 609$.

Это означает, что при большей части наборов параметров система успевает достичь насыщения за выбранный интервал времени.

Исключение наблюдается во второй модели при малом значении параметра $a$. В этом случае итоговое значение $n_{final}$ оказывается ниже предельного уровня.

## Зависимость $n_{final}$ от параметра $b$

![Зависимость итогового значения n от параметра b](image/n_final_vs_b.png)

## Анализ зависимости $n_{final}(b)$

Параметр $b$ определяет силу влияния общения между покупателями.

При больших значениях $b$ информация распространяется быстрее, поскольку вклад уже информированных покупателей становится значительнее.

Если значение $b$ недостаточно велико, система может не успеть выйти на уровень насыщения за заданное время моделирования.

# Максимальные значения

## Зависимость $n_{max}$ от параметра $a$

![Зависимость максимального значения n от параметра a](image/n_max_vs_a.png)

## Зависимость $n_{max}$ от параметра $b$

![Зависимость максимального значения n от параметра b](image/n_max_vs_b.png)

## Анализ $n_{max}$

Поскольку решения в базовых экспериментах возрастают монотонно, величина $n_{max}$ практически совпадает с $n_{final}$.

Если модель достигает насыщения, то выполняется приближённое равенство:

$$
n_{max} \approx N.
$$

Если расчётного времени недостаточно, значение $n_{max}$ остаётся ниже $N$.

# Финальное насыщение

## Зависимость насыщения от параметра $a$

![Зависимость финального насыщения от параметра a](image/saturation_final_vs_a.png)

## Зависимость насыщения от параметра $b$

![Зависимость финального насыщения от параметра b](image/saturation_final_vs_b.png)

## Метрика насыщения

Для оценки степени достижения предельного уровня использовалась метрика:

$$
\text{saturation\_final} =
\frac{n_{final}}{N}.
$$

Если её значение близко к $1$, то система почти полностью достигла уровня насыщения.

## Интерпретация насыщения

Для большинства экспериментов выполняется соотношение:

$$
\frac{n_{final}}{N} \approx 1.
$$

Это означает почти полный охват потенциальной аудитории.

Пониженные значения показывают, что процесс распространения рекламы ещё не завершился к концу расчётного интервала.

# Бенчмаркинг

## Время решения от параметра $a$

![Зависимость времени решения ODE от параметра a](image/computation_time_vs_a.png)

## Время решения от параметра $b$

![Зависимость времени решения ODE от параметра b](image/computation_time_vs_b.png)

## Анализ времени вычислений

Результаты бенчмаркинга показывают:

- все три модели решаются быстро;
- время вычислений имеет порядок $10^{-5}$ секунды;
- изменение параметров почти не влияет на вычислительную сложность;
- третья модель требует немного больше вычислений из-за функций $\cos t$ и $\cos 2t$.

# Итоги

## Основные результаты

1. Все три модели описывают насыщаемый рост числа информированных покупателей.
2. Предельным уровнем охвата является $N = 609$.
3. Первая модель имеет максимальную скорость роста в начальный момент времени.
4. Вторая модель демонстрирует наиболее резкий S-образный переход к насыщению.
5. Третья модель учитывает изменение коэффициентов во времени.

## Выводы

1. Первая модель (case1) описывает монотонное приближение $n(t)$ к уровню $N$.

2. Вторая модель (case2) демонстрирует S-образный рост и имеет внутренний максимум скорости распространения рекламы.

3. Третья модель (case3) также приводит систему к насыщению, но использует коэффициенты, зависящие от времени.

4. Параметры $a$ и $b$ главным образом влияют на скорость выхода к уровню насыщения.

5. Метрики $n_{final}$, $n_{max}$ и $\text{saturation\_final}$ позволяют количественно сравнить результаты моделирования.

6. Численное решение всех трёх моделей выполняется эффективно и не требует значительных вычислительных ресурсов.

# Список литературы {.unnumbered}

1. [Модель Мальтуса](http://km.mmf.bsu.by/courses/2018/mathmod1/MM_LB1_Population_2019.pdf)
2. [Логистическая модель роста](https://studopedia.ru/29_5129_logisticheskaya-model-rosta.html)
